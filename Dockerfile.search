FROM us-central1-docker.pkg.dev/videosearch-comparison/videosearch/webapp-search-base:v1

WORKDIR /app

COPY app/ app/
COPY static/ static/

ENV PORT=8080
ENV SEARCH_BACKEND=gguf
ENV DEVICE=cpu
EXPOSE 8080

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
